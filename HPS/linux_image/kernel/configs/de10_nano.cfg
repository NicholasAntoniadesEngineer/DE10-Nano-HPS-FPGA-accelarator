# DE10-Nano custom kernel config fragment
# Applied on top of socfpga_defconfig during kernel-config step.
# Add options here instead of editing the defconfig directly.

# UIO (Userspace I/O) — required for FPGA IP register access without /dev/mem
CONFIG_UIO=y
CONFIG_UIO_PDRV_GENIRQ=y
